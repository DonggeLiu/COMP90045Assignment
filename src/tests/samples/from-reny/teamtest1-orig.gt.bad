proc main()
    int a;
    int b[50];
    float c[100,2];
    bool d[23];
begin
    new_state's := something [ab [cd, ef]];
    new[hello] := ((6 * i) + 4) - i;
    read yes [May [the, force]];
    while a+a/b
    do
      if newew[false]
        then read ss;
        read ff;
      else
        call a ("if then' else", "1234");
      fi

    od
end
