proc p (val float n)
begin
    write n;
end

proc main ()
    int a[5, 3];
    int b;
begin
    a[5, 1] := ((((-2 * (1 - 10)) + 2) + (2 / 2)) + (14 * 5.4)) - i;
    if ((a >= 1) && (a <= 3)) || (a > 5) then
        a := 3;
    fi
    while n > 1 do
        call p(n[2, 1]);
    od
    write a;
    write "\n";
    b := -a + a;
    write b;
    write "\n";
end
