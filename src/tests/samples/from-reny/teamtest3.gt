proc main()
    int a;
    int b[50];
    float c[100,2];
    bool d[23];
begin
    newstates := something [ab [cd, ef]];
    new[hello,hey [(some [1+1=2]), thing]] := ((6 * i) + 4) - i;
    read yes [May [the, force]];
    while a+a/b
    do
      if newew[false]
        then read ss;
        read ff;
      else
        # call a("if then                                  else", "1234");
        # EDIT: We intentionally decided that we should not allow
        # strings as part of expressions.
        call a (else', 1234);
        write "if then                                  else";
      fi

    od
end
