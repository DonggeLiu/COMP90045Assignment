proc main()

begin
    newstates := something [ab [cd, ef]];
    new[hello,hey [(some [1+1=2]), thing]] := ((6 * i) + 4) - i;
    read yes [May [the, force]];
    while a+a/b
    do
      if newew[false]
        then read ss;
        read ff;
      else
        # call a ("if then else", x=y=z, b||c, d&&e);  #nice
        # EDIT: relational operators are not meant to be associative:
        call a ("if then else", x=y, b||c, d&&e);  #nice
      fi

    od
end

proc seco_'nd(val bool a, ref float b, ref int c)

begin
newstates := something [ab [cd, ef]];
end
